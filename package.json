{
  "name": "k8s-ci-cd-demo",
  "version": "1.0.0",
  "description": "Demo application for CI/CD with Kubernetes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
